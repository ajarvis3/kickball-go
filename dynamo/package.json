{
   "name": "dynamo",
   "version": "1.0.0",
   "type": "module",
   "main": "index.js",
   "license": "MIT",
   "scripts": {
      "build": "tsc -p tsconfig.json",
      "synth": "yarn build && cdk synth",
      "bootstrap": "cdk bootstrap",
      "deploy": "cdk deploy",
      "SBD": "yarn build && yarn synth && yarn bootstrap && yarn deploy",  
      "down": "cdk destroy"
   },
   "dependencies": {
      "aws-cdk-lib": "^2.248.0",
      "ci": "^2.3.0",
      "constructs": "^10.6.0"
   },
   "devDependencies": {
      "@types/node": "^25.5.2",
      "aws-cdk": "^2.1117.0",
      "ts-node": "^10.9.2",
      "typescript": "^6.0.2"
   }
}
